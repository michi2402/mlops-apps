# lakeFS serves Prometheus metrics on its API port.
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lakefs
  namespace: platform-lakefs
spec:
  selector:
    matchLabels:
      app: lakefs
  endpoints:
    - port: http
      path: /metrics
      interval: 30s

apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: local-example
  namespace: argocd
spec:
  description: Local example project grouping platform and workloads apps
  sourceRepos:
    - '*'
  destinations:
    - namespace: '*'
      server: https://kubernetes.default.svc
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
